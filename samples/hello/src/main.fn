
# My comment
#{
My
multi-line
comment
}

# a 'let' binding allows the compiler to perform comptime optimizations like value inlining
let num #{u64} = 7;

public main = .() {
    var local = num;
    var local2 = local;
    local; # IR optimizer removes this dead code
    local2
}
