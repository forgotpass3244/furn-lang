
# My comment
#{
My
multi-line
comment
}

# a `let` binding allows the compiler to perform comptime optimizations like value inlining
let num: u64 = 67; # type annotation optional

let func = .() { num };

public main = .() {
    print("hello\n");
    func()
}
